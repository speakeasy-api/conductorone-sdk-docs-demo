```typescript C1ApiUserV1UserSearchSearch.ts
import { ConductoroneSDKTypescript } from "conductorone-sdk-typescript";
import { SearchUsersRequestUserStatuses } from "conductorone-sdk-typescript/dist/sdk/models/shared";

(async() => {
  const sdk = new ConductoroneSDKTypescript({
    security: {
      bearerAuth: "",
      oauth: "",
    },
  });

  const res = await sdk.userSearch.search({
    userExpandMask: {
      paths: [
        "string",
      ],
    },
    excludeIds: [
      "string",
    ],
    ids: [
      "string",
    ],
    refs: [
      {},
    ],
    roleIds: [
      "string",
    ],
    userStatuses: [
      SearchUsersRequestUserStatuses.Disabled,
    ],
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
