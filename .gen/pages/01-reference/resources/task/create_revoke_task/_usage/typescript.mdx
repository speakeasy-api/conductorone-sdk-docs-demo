```typescript CreateRevokeTask.ts
import { ConductoroneSDKTypescript } from "conductorone-sdk-typescript";

(async() => {
  const sdk = new ConductoroneSDKTypescript({
    security: {
      bearerAuth: "",
      oauth: "",
    },
  });

  const res = await sdk.task.createRevokeTask({
    taskExpandMask: {
      paths: [
        "string",
      ],
    },
    appEntitlementId: "string",
    appId: "string",
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
