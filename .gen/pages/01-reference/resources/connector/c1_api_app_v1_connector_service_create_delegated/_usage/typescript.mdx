```typescript C1ApiAppV1ConnectorServiceCreateDelegated.ts
import { ConductoroneSDKTypescript } from "conductorone-sdk-typescript";

(async() => {
  const sdk = new ConductoroneSDKTypescript({
    security: {
      bearerAuth: "",
      oauth: "",
    },
  });

  const res = await sdk.connector.createDelegated({
    connectorServiceCreateDelegatedRequest: {
      connectorExpandMask: {
        paths: [
          "string",
        ],
      },
      userIds: [
        "string",
      ],
    },
    appId: "string",
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
