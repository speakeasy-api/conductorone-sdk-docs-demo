
<CH.Code>
```typescript AddAccessEntitlements.ts
import { SDK } from "openapi";
import { AppEntitlementRef, RequestCatalogManagementServiceAddAccessEntitlementsRequest } from "openapi/dist/models/components";
import { C1ApiRequestcatalogV1RequestCatalogManagementServiceAddAccessEntitlementsRequest } from "openapi/dist/models/operations";

async function run() {
  const sdk = new SDK({
    security: {
      bearerAuth: "",
      oauth: "",
    },
  });
const catalogId: string = "string";
const requestCatalogManagementServiceAddAccessEntitlementsRequest: RequestCatalogManagementServiceAddAccessEntitlementsRequest = {
  accessEntitlements: [
    {},
  ],
};

  const res = await sdk.requestCatalogManagement.addAccessEntitlements(catalogId, requestCatalogManagementServiceAddAccessEntitlementsRequest);

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
```
---

```json Example Response
  {}
```
</CH.Code>
