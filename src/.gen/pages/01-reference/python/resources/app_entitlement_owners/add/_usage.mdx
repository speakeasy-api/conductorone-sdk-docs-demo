
<CH.Code>
```python Add.py
import sdk
from sdk.models import components, operations

s = sdk.SDK(
    security=components.Security(
        bearer_auth="",
        oauth="",
    ),
)


res = s.app_entitlement_owners.add(app_id='string', entitlement_id='string', add_app_entitlement_owner_request=components.AddAppEntitlementOwnerRequest())

if res.add_app_entitlement_owner_response is not None:
    # handle response
    pass
```
---

```json Example Response
  {}
```
</CH.Code>
