
<CH.Code>
```python Remove.py
import sdk
from sdk.models import components, operations

s = sdk.SDK(
    security=components.Security(
        bearer_auth="",
        oauth="",
    ),
)


res = s.app_owners.remove(app_id='string', user_id='string', remove_app_owner_request=components.RemoveAppOwnerRequest())

if res.remove_app_owner_response is not None:
    # handle response
    pass
```
---

```json Example Response
  {}
```
</CH.Code>
