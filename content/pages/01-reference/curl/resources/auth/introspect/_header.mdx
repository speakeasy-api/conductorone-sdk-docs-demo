import OperationInfo from '/src/components/OperationInfo';

## Introspect

<OperationInfo method='get' path='/api/v1/auth/introspect' />

Introspect returns the current user's principle_id, user_id and a list of roles, permissions, and enabled features.
