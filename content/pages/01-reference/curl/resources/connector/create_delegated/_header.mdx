import OperationInfo from '/src/components/OperationInfo';

## Create Delegated

<OperationInfo method='post' path='/api/v1/apps/{app_id}/connectors' />

Create a connector that is pending a connector config.
