```typescript SearchAttributeValues.ts
import { ConductoroneSDKTypescript } from "conductorone-sdk-typescript";

(async() => {
  const sdk = new ConductoroneSDKTypescript({
    security: {
      bearerAuth: "",
      oauth: "",
    },
  });

  const res = await sdk.attributeSearch.searchAttributeValues({
    attributeTypeIds: [
      "string",
    ],
    excludeIds: [
      "string",
    ],
    ids: [
      "string",
    ],
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
