{/* Autogenerated DO NOT EDIT  */}
import Collapsible from "/src/components/Collapsible";
import Labels from "/src/lib/labels";

##### `acceptInstance` _object (optional)_
This policy step indicates that a ticket should have an approved outcome. This is a terminal approval state and is used to explicitly define the end of approval steps.
 The instance is just a marker for it being copied into an active policy.
<Collapsible content={() => import('/content/types/shared/accept_instance/curl.mdx')} openLabel={Labels.showProperties} closeLabel={Labels.hideProperties}>
</Collapsible>

---
##### `approvalInstance` _object (optional)_
The approval instance object describes the way a policy step should be approved as well as its outcomes and state.

This message contains a oneof named outcome. Only a single field of the following list may be set at a time:
  \- approved
  \- denied
  \- reassigned
  \- restarted
  \- reassignedByError

<Collapsible content={() => import('/content/types/shared/approval_instance/curl.mdx')} openLabel={Labels.showProperties} closeLabel={Labels.hideProperties}>
</Collapsible>

---
##### `provisionInstance` _object (optional)_
A provision instance describes the specific configuration of an executing provision policy step including actions taken and notification id.

This message contains a oneof named outcome. Only a single field of the following list may be set at a time:
  \- completed
  \- cancelled
  \- errored
  \- reassignedByError

<Collapsible content={() => import('/content/types/shared/provision_instance/curl.mdx')} openLabel={Labels.showProperties} closeLabel={Labels.hideProperties}>
</Collapsible>

---
##### `rejectInstance` _object (optional)_
This policy step indicates that a ticket should have a denied outcome. This is a terminal approval state and is used to explicitly define the end of approval steps.
 The instance is just a marker for it being copied into an active policy.
<Collapsible content={() => import('/content/types/shared/reject_instance/curl.mdx')} openLabel={Labels.showProperties} closeLabel={Labels.hideProperties}>
</Collapsible>

---
##### `id` _string (optional)_
The ID of the PolicyStepInstance. This is required by many action submission endpoints to indicate what step you're approving.

---
##### `policyGenerationId` _string (optional)_
The policy generation id refers to the version of the policy that this step was created from.

---
##### `state` _enumeration (optional)_
The state of the step, which is either active or done.
<Collapsible content={() => import('/content/types/shared/policy_step_instance_state/curl.mdx')} openLabel={Labels.showProperties} closeLabel={Labels.hideProperties}>
</Collapsible>


